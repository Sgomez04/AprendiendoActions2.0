name: Update Setup Version

on:
  # pull_request:
  push:
    branches:
    - 'main'

jobs:
  update_version:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: "Actualizar versión en setup.py"
        run: |
          sed -i "s/version=[\"'][0-9.]*[\"']/version=pepito/g" "$setup_py" 
          cat setup.py